import 'package:flutter/material.dart';

class CardArtistViews extends StatelessWidget {
  const CardArtistViews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
